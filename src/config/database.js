// Database configuration and utilities
// Prisma client is now initialized in UserService for better connection management

module.exports = {
  // Add any database-specific configuration here if needed
  // For now, Prisma client is managed in the services
};
